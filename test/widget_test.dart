// Smoke test tối thiểu cho NovelVerse (prototype). Test mặc định của Flutter
// tham chiếu MyApp không tồn tại nên thay bằng test placeholder.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
